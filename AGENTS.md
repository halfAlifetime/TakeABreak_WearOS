# 项目设计原则

以下原则适用于本项目的代码、测试与技术文档。

## 1. Codex 可读性

- 让 Codex 和开发者能从代码及必要的局部上下文理解功能目的、执行流程和修改影响，不依赖聊天记录或隐含约定。
- 使用清晰、一致的文件名、类型名和函数名；明确输入、输出、状态变化及副作用，避免含糊缩写、魔法值和隐藏依赖。
- 保持控制流程直接、职责集中。复杂状态转换、异常处理和恢复逻辑应说明约束及设计原因，注释避免重复代码表面含义。
- 相关实现、测试和说明应便于定位；修改行为时同步更新相关说明，确保文档与代码一致。

## 2. 功能模块化设计

- 按功能职责组织代码，使每个模块有明确边界和稳定入口；同一业务规则由一个明确的模块负责。
- 分离界面展示、计时业务、数据持久化与系统能力。界面通过业务入口执行操作，通知、振动、权限和闹钟等平台细节封装在对应模块内。
- 通过显式参数或必要接口表达依赖，避免跨模块直接修改内部状态、循环依赖及重复实现业务规则。
- 模块应便于独立理解、验证和替换；根据实际复杂度拆分，避免仅为形式增加层级、接口或 Gradle 模块。
