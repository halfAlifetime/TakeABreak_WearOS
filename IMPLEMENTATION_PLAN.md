# 原生 Wear OS 项目结构优化实施计划

编写日期：2026-09-23。

适用工程：`C:/php/3/TakeABreak_WearOS`。

依据：[AGENTS.md](./AGENTS.md) 中的“Codex 可读性”和“功能模块化设计”，以及本轮结构审查发现的六项问题。

状态：实施中。按用户要求由单一代理执行；用户已明确要求先完成本地重构，真机测试稍后进行。下方计划描述目标，实际完成情况以第 13 节执行记录为准。

**1. 目标、基线与范围**

目标是让提醒规则有明确归属、时长修改不会携带旧值覆盖其他字段、业务恢复不依赖中文文案，并使计时核心与界面组件能够分别理解和验证。

当前基线：

| 项目 | 当前状态 |
| --- | --- |
| 最近代码提交 | `ee40b31`：修复提醒并加强计时恢复 |
| 工程形态 | 单个 `app` Gradle 模块，保留现有 `timer`、`alarm`、`notification`、`permission`、`ui` 分包 |
| 计时引擎 | `TimerEngine.kt` 879 行，统一管理有效状态和命令互斥 |
| 主页面 | `TimerScreen.kt` 921 行，包含水波绘制、各状态布局和弹窗 |
| 最近本地验证 | 67 项单元测试通过；构建成功；Lint 0 错误、34 条警告 |
| 最近真机验证 | SM-L310 上手动测试及工作、休息两次自动提醒均有用户触感确认 |
| 调试安装 | 使用 `-PisolatedDebug=true`，包名 `com.takeabreak.wearos.debug`，名称“休息一下·调试” |
| 计划编写时 Git 状态 | `AGENTS.md` 尚未提交；本计划新增后也属于未提交文件 |

上述行数、测试数和结果是计划编写时的基线，实施后按实际结果更新。

实施范围包括源码组织、必要接口调整、针对性回归测试及相关文档。继续采用现有依赖和手工依赖组装；不以文件数量或行数作为验收指标。本轮不增加计时功能，也不调整当前页面视觉设计。

**2. 全过程必须保持的行为**

1. `TimerEngine` 继续是计时有效状态的唯一修改入口。仓库提供持久化，界面和 Receiver 不直接写计时状态。
2. 所有命令继续经过同一把 `Mutex`。等待锁可取消；进入副作用提交阶段后保持现有 `NonCancellable` 与 IO 执行边界，完成提交或补偿后再释放锁。
3. 保留停止记录、会话恢复许可、按会话停止和 generation 校验。旧通知、晚到广播不能停止新会话或恢复已停止会话。
4. 到点依据继续是系统闹钟。界面 ticker 仅刷新显示，不推进阶段。
5. 同次开机按 elapsedRealtime 计算剩余量；真实重启按现有规则恢复，已过期阶段等待用户确认，不追赶所有错过的阶段。
6. 正式振动只在有效阶段切换提交后执行，工作和休息沿用现有节奏与 USAGE_ALARM。手动自检不推进计时，测试通知保持静默。
7. 振动请求成功仅代表请求已发出，不能显示为物理触感已经确认。正式通知的显示和全屏配置保留，其他设备是否叠加渠道振动仍作为设备验证边界记录。
8. 保留通知 action、渠道 ID、会话参数、PendingIntent 身份、现有 DataStore 与停止记录键的兼容性。
9. 手表原应用与调试应用继续并存。真机短周期验证结束后恢复测试前的时长和设备设置，清理测试会话的闹钟与通知。

**3. 实施顺序与提交边界**

按 `P0 → P1 → P2 → P3 → P4 → P5` 串行实施。每一阶段完成对应验证后再进入下一阶段。

| 阶段 | 交付物 | 完成条件 | 建议提交 |
| --- | --- | --- | --- |
| P0 基线确认 | 工作区、测试和设备证据清单 | 基线可追溯，现有用户改动已识别 | 并入 P1 的说明 |
| P1 提醒能力统一 | 能力快照、统一策略、页面与执行接入 | 同一条件下诊断与振动决策一致 | `refactor: unify reminder capability policy` |
| P2 时长原子修改 | 单字段命令、锁内合并、竞态回归 | 两种修改顺序均不丢失另一字段 | `fix: update timer durations atomically` |
| P3 计时核心整理 | 纯阶段规则、恢复策略、类型化故障 | 原有恢复与取消语义保持，旧数据可读 | `refactor: separate timer rules and recovery policy` |
| P4 界面与依赖整理 | 页面组件、显式 ViewModel 依赖、统一反馈 | 外观交互保持，ViewModel 可独立测试 | `refactor: modularize timer UI and dependencies` |
| P5 命名与最终验收 | 正式振动与自检命名、文档、最终实测 | 全部检查通过，设备清理完成 | `refactor: clarify reminder modules and update documentation` |

以上是五个实施提交边界。执行时保持每个提交可构建、可验证，并使说明与最终改动一致。设备安装与测试按用户最新安排延后。

**4. P0：确认基线与准备验证材料**

- [x] 读取 `AGENTS.md`，确认工程路径、分支和当前提交，检查工作区是否出现计划之外的新修改。
- [x] 确认上述 67 项测试报告对应的源码状态；如果源码已变化，重新执行基线测试和构建并记录结果。
- [x] 记录现有应用的工作/休息节奏、停止行为、通知动作与恢复语义，链接 `DEVICE_DEBUG_REPORT.md` 中的证据。
- [x] 保留主页面运行、暂停、停止、错误状态以及两类弹窗的现有截图或可复现操作，作为 P4 的视觉对照。
- [x] 将本计划中的状态统一保持为“未开始”，后续按阶段记录实际完成项及验证结果。

P0 验收：可以明确区分现有行为、已确认缺陷和尚待复现的风险。时长覆盖问题当前属于静态调用链发现的竞态风险，需在 P2 中建立确定性回归场景。

**5. P1：统一提醒能力读取与判断**

当前问题：`ReminderPermissionChecker.checkPreflight()`、`evaluate()`、`AndroidReminderVibration.blockedReason()` 和状态页分别持有部分规则，导致勿扰、渠道重要性等条件的解释不同。

拟改文件及职责：

| 文件 | 工作内容 |
| --- | --- |
| 新增 `permission/ReminderCapabilities.kt` | 定义不可变能力快照、渠道快照及结构化问题代码；不依赖 Android 类型或中文文案 |
| 新增 `permission/ReminderCapabilityReader.kt` | 声明读取能力快照的接口 |
| 新增 `permission/AndroidReminderCapabilityReader.kt` | 读取通知权限、渠道、分组、勿扰、振动器及全屏权限等系统信息 |
| 新增 `permission/ReminderPolicy.kt` | 根据用途输出允许、阻止原因和提示信息；纯函数，不执行系统操作 |
| `permission/ReminderPermissionChecker.kt` | 迁移旧规则；过渡方法仅委托统一策略，不保留独立判断副本 |
| 新增 `permission/ReminderSettingsNavigator.kt` | 承接旧检查器中的系统设置 Intent 构造，保留原有回退入口 |
| `notification/AndroidReminderVibration.kt` | 振动前委托统一策略，保留底层振动 API、节奏和取消行为 |
| `ui/TimerViewModel.kt`、`ui/ReminderStatusScreen.kt` | 显示同一快照的判断结果，去除页面自行比较 `importance >= 4` 等规则 |
| `notification/NotificationActionReceiver.kt` | 通知继续使用相同的命令前置策略，会话校验仍在引擎锁内完成 |
| `alarm/AlarmScheduler.kt`、`TakeABreakApplication.kt` | 复用现有精确闹钟能力查询并组装依赖，避免再次复制系统权限判断 |

能力快照至少包含：精确闹钟能力、通知总开关与运行时授权、各渠道存在性/重要性/振动开关/分组状态、勿扰状态、振动器可用性及全屏通知授权状态。系统读取失败使用明确的未知或不可用状态，不能默认为已就绪。

策略按用途区分：

- 命令前置检查：决定当前是否允许开始、继续或重试，并返回触感或弹屏受限的提示。
- 振动执行检查：仅检查当前阶段目标渠道及直接振动所需条件；手动工作自检不因休息渠道关闭而失败。
- 展示诊断：分别显示调度配置、振动请求许可和弹屏相关配置，不将它们压缩成一个“完全就绪”布尔值。

以下表格是本轮实施的预期行为，除列出的限制外假定其他条件均满足。勿扰采用现有直接振动路径的保守策略，不改变手表全局设置。

| 条件 | 开始/继续/重试 | 直接振动请求 | 页面说明 |
| --- | --- | --- | --- |
| 精确闹钟权限缺失 | 阻止 | 单独手动自检仍可执行 | 无法保证准时调度 |
| 通知总开关关闭或通知权限缺失 | 阻止 | 阻止 | 应用通知不可用 |
| 任一必要阶段渠道缺失、关闭、振动关闭或分组关闭 | 阻止 | 按目标渠道分别判断 | 标明具体阶段与原因 |
| 渠道为 LOW/MIN，但存在且振动开关开启 | 允许计时，保留降级提醒 | 阻止 | 当前渠道不足以执行触感提醒 |
| 渠道为 DEFAULT，振动开启 | 允许 | 允许 | 振动请求可用，弹屏配置不足 |
| 渠道为 HIGH，振动开启，勿扰为 ALL | 允许 | 允许 | 配置满足条件，触感需实际确认 |
| 勿扰为 ALARMS | 允许 | 允许 ALARM 用途请求 | 显示当前勿扰状态，不保证系统弹屏 |
| 勿扰为 PRIORITY 或 NONE | 允许计时，明确提示触感受限 | 阻止 | 显示勿扰限制，不显示触感已就绪 |
| 没有可用振动器 | 允许计时，明确提示无触感 | 阻止 | 设备振动能力不可用 |
| 必要能力读取失败 | 不宣称能力满足；相关操作返回不可验证原因 | 不发出无法确认许可的请求 | 显示读取失败，通过现有自检或返回页面操作重新读取 |

高重要性、全屏权限和系统展示结果分别记录；仅凭这些配置不能承诺一定弹屏。运行中的阶段不会因诊断页刷新而被自动停止；原有调度失败处理仍由引擎负责。

实施步骤：

1. 定义能力数据和问题代码，明确每个字段的来源及未知值语义。
2. 建立纯策略测试，再接入 Android 读取器。精确闹钟查询复用调度适配器的实现；执行调度时仍重新检查权限，处理状态变化。
3. 按“诊断 → 手动自检 → 正式振动 → UI/通知命令前置检查”的顺序迁移调用方。
4. 页面显示结构化问题对应的中文说明，文案映射集中管理；执行逻辑不解析说明文字。
5. 每次振动前重新读取能力，避免启动时通过检查后持续使用旧结论。
6. 复查既有系统恢复入口，明确它与用户命令的不同用途，不为统一接口擅自增加自动恢复或自动停止行为。
7. 当所有调用方迁移完成后删除旧检查器中的重复规则。修正 README 与直接振动实现冲突的说明，文档不等待 P5 才纠正。

新增验证：

- 参数化验证上表，包括工作/休息渠道独立判断、重要性边界和分组关闭。
- 同一快照下，页面诊断与执行策略引用同一个阻止原因。
- 第一次允许后切换设置，第二次请求重新判断并受到阻止。
- 保留现有振动器缺失、请求异常、重复自检、静默通知发送失败测试。
- Android 读取器的字段映射通过调试包设备验证；纯策略测试不能替代系统状态映射验证。

P1 验收：规则只有一处定义；优先勿扰状态不会出现“允许计时却没有任何触感受限说明”的情况；正常设置下手动与自动振动调用路径保留。公共接口不泄露内部类型，模块可正常编译。

**6. P2：以单字段命令原子修改时长**

拟改文件：`ui/TimerViewModel.kt`、`timer/TimerEngine.kt`、`timer/TimerRepository.kt` 及对应测试。

对界面提供：

```kotlin
suspend fun updateWorkDuration(minutes: Int): Result<Unit>
suspend fun updateBreakDuration(minutes: Int): Result<Unit>
```

处理流程固定为：用户提交变动字段 → 获取引擎锁 → 读取最新有效状态 → 验证 STOPPED 与合法时长 → 合并字段 → 保存 → 发布保存后的状态。

实施步骤：

1. 使用现有 Fake 仓库的可挂起写入能力，建立旧快照覆盖场景，记录预期最终值。
2. 增加两个单字段入口，共用引擎内部合并与校验逻辑。界面不再读取另一项时长并拼成完整更新请求。公共入口只获取一次锁，锁内辅助函数不得再调用另一个已加锁入口。
3. 原批量 `updateDurations()` 若仍供内部配置或测试使用，收窄为 `internal`，与单字段入口共享实现；禁止 UI 使用批量入口回传旧快照。
4. 更新成功时，STOPPED 下的 `phaseTotalDurationMs` 与最终工作时长保持一致。
5. 保存失败时保留修改前有效状态，反馈失败，不发布未经保存的偏好。
6. 确认无调用后删除仓库 `saveDurations()`，同步调整 Fake 仓库。对外的状态与业务修改继续通过引擎进行。

新增验证：

| 用例 | 操作 | 预期 |
| --- | --- | --- |
| D1 | 初始 60/5；挂起工作 45 的写入，再提交休息 10，最后释放写入 | 最终 45/10 |
| D2 | 交换上述工作和休息操作顺序 | 最终仍为 45/10 |
| D3 | 连续修改同一字段 | 按引擎接收并串行执行的命令顺序，后一次成功修改生效 |
| D4 | 修改开始排队后，其他命令使状态不再是 STOPPED | 获锁后重新校验并拒绝，不修改运行中的时长 |
| D5 | 输入 0 或负值 | 拒绝，状态和存储保持原值 |
| D6 | 保存持续失败 | 返回失败，内存与已保存偏好不被伪更新 |

竞态测试采用明确的挂起点和释放信号，不依赖固定 sleep 或手动点击速度。P4 再补 ViewModel 层验证，确认 UI 只传一个字段。

P2 验收：D1、D2 确定性通过，原有时长失败测试继续有效；界面已无通过旧快照回传另一字段的调用。

**7. P3：提取计时规则与类型化故障**

这一步风险最高，按下列顺序逐项改动并验证，避免同时移动所有事务分支。

7.1 明确故障类型与旧数据兼容

- 新增 `timer/TimerFailure.kt`，使用稳定的故障代码表达读取失败、写入失败、闹钟调度失败和未知故障。
- 在 `TimerState` 增加可选故障原因字段，`errorMessage` 继续用于展示和诊断。
- DataStore 新增可选的字符串键，例如 `failure_reason`。使用稳定代码，不保存 enum ordinal；现有键保持不变。
- 读取旧 ERROR 数据且没有新字段、或遇到未知代码时映射为 UNKNOWN；非 ERROR 状态不采用残留的故障原因。
- 每个进入 ERROR 的分支明确设置原因；成功开始、继续、停止或恢复时清理故障原因与错误信息。
- 精确闹钟权限恢复时，只对明确的调度类故障执行既有自动重试。旧 UNKNOWN 错误保留手动重试，不再解析中文文案推断自动恢复资格。

7.2 提取纯阶段规则

- 新增 `timer/TimerTransitions.kt`，集中计算 WORK/BREAK 切换、轮数递增、目标时长、到期后继续的目标阶段。
- 替换 `resumeLocked()` 与 `onPhaseAlarm()` 中重复的阶段计算。
- 输入为计时状态及必要时间值，输出为明确的目标信息；不持有 Context、不读仓库、不排闹钟。
- 不随提取改变取整、提前 1 秒防护、到期确认及轮数语义。

7.3 提取纯恢复策略

- 新增 `timer/TimerRecoveryPolicy.kt`，集中判断同次开机、真实重启、剩余时间和到期等待确认。
- 所需状态、时钟值及停止记录判断结果均由引擎在锁内获取后传入。
- 策略只返回继续排程、等待确认或保持状态等决策。停止日志落盘、撤销闹钟、保存状态和通知更新的执行顺序仍由引擎负责。
- 将 Android 系统 action 在 Receiver 边界映射为明确的事件类型，计时规则不再依赖散落的 action 字符串。
- 对仅为诊断记录的事件描述与用于分支判断的事件类型分别建模。

7.4 整理接口和实现

- `TimerRepository.kt` 保留仓库接口，DataStore 实现移至 `DataStoreTimerRepository.kt`。
- `StopIntentStore.kt` 保留停止记录接口，SharedPreferences 实现移至 `SharedPreferencesStopIntentStore.kt`。
- 业务接口继续以输入输出和异常约定说明行为；必要时为系统时钟实现做同类文件拆分。
- 引擎保留唯一有效状态流和原互斥边界。不得让提取出的策略各自维护状态、锁或后台协程。
- 仅提取语义一致的重复计算。开始、阶段切换和停止的不同提交顺序不得压成一个忽略差异的通用事务模板。

新增与保留验证：

- 阶段规则：工作进入同轮休息、休息进入下一轮工作、暂停有剩余时间时继续、暂停为 0 时确认进入下一阶段。
- 故障恢复：修改中文提示后相同行为保持；调度故障与存储故障在权限恢复事件下的处理不同。
- 持久化兼容：缺少 `failure_reason`、合法新代码、未知代码、非 ERROR 残留代码；新字段能够保存并重新读取。
- 恢复策略：同次开机改时、重启未到期、重启已到期、BOOT_COUNT 缺失时的原兼容路径。
- 全量运行原有停止许可、通知目标会话、读写失败、进程重建、旧广播隔离及事务取消测试。

旧数据映射测试可以针对提取的纯代码转换函数执行；实际 DataStore 字段读写必须另做集成验证。若使用设备数据夹具，只在停止的独立调试包上操作，先保存原始数据，以 adb push 传输二进制并校验回读，完成后恢复。禁止重用此前会在 Windows stdin 的 0x1A 处截断的传输方式。

P3 验收：中文错误信息不再参与业务分支；新旧数据均可加载；既有故障和取消保护测试没有因重构被删减或放宽断言。

**8. P4：拆分界面并显式注入依赖**

拟形成的界面职责：

| 文件 | 职责 | 不承担的操作 |
| --- | --- | --- |
| `ui/TimerScreen.kt` | 组合页面，连接状态与回调，控制局部弹窗可见性 | 直接写存储、排闹钟或读取系统权限 |
| `ui/TimerWaterBackground.kt` | 水波路径、色彩及动画绘制 | 推进计时阶段 |
| `ui/TimerContent.kt` | 倒计时、阶段和状态文字 | 推断错误原因或恢复规则 |
| `ui/TimerControls.kt` | 设置与计时操作按钮，保留触控区域和无障碍描述 | 持有引擎或 Application |
| `ui/TimerDialogs.kt` | 操作反馈、停止确认及模态行为 | 根据中文文字猜测反馈类型 |
| `ui/TimerViewModel.kt` | 调用业务入口，维护界面状态、反馈及导航意图 | 自行修改仓库或重复判断提醒策略 |

组件默认使用 `private` 或 `internal`，只有确有跨文件用途时才扩大可见性。共享视觉组件只提取实际重复部分，保留不同状态的清晰分支。

实施步骤：

1. 先按现有代码原样提取背景与弹窗，再提取重复按钮和计时内容。每次提取后检查预览或真机显示，避免混入视觉调整。
2. 水波动画继续只在 RUNNING 且页面 RESUMED 时运行；提取不得导致隐藏页面持续启动无限动画。
3. 主页面仅接收 `ActionFeedback?`。删除 `actionMessage`、`actionSettingTarget` 兼容参数及文字猜测反馈类型的分支；提醒状态页也消费明确的反馈模型。
4. 将 `TimerViewModel` 从主动读取 `TakeABreakApplication` 改为构造参数接收引擎、时钟、能力读取器及手动自检能力。
5. 使用轻量 ViewModel Factory，由 `TakeABreakApplication` 和 `MainActivity` 组装现有实例。无需引入新的依赖注入框架。
6. 对外只暴露只读的 `State`/`StateFlow`，包括当前直接暴露 MutableState 的渠道信息。
7. 保留导航命令消费、前台 ticker 启停、通知点击返回当前计时页面和短暂亮屏释放行为。

验证安排：

- 新增 ViewModel 测试：单字段时长命令、权限限制反馈、手动自检成功/失败反馈、导航命令只消费一次。
- 使用现有协程测试依赖控制主线程调度与时间；引擎原有真实 IO 调度的用例仍使用明确同步信号，不假定虚拟时间能控制全部执行器。
- 视觉对照 STOPPED、RUNNING、PAUSED、ERROR 四种状态。核对倒计时位置、圆形按钮尺寸、圆屏裁切、长文本和颜色。
- 操作验证停止弹窗外部点击不穿透、返回键关闭弹窗、设置页返回、权限设置返回后刷新、通知导航不重置计时。
- 检查页面不可见或暂停时水波动画停止；自检反馈保持在按钮附近。

P4 验收：组件能够从参数看懂职责；ViewModel 无需真实 Application 即可进行行为测试；页面交互与 P0 基线一致。

**9. P5：明确正式提醒与自检命名，完成交付验证**

实施步骤：

1. 将 `ReminderVibrationTest.kt` 中正式提醒与自检共用的振动请求接口和逻辑移至 `notification/ReminderVibration.kt`。
2. 手动自检协调保留在 `notification/ReminderSelfTest.kt`，对应结果类型按自检用途命名；测试文件同步改名，避免出现 `TestTest`。
3. 正式阶段与自检继续复用同一振动请求路径。自检的静默通知不进入正式通知流程，阶段提交前不发送正式振动。
4. 检查 `ReminderNotifier` 的接口边界，将仅用于手动自检的入口从计时引擎依赖的接口中分离；实现通过现有组件组合，避免引擎 Fake 为自检接口提供无用实现。
5. 检查通知清理仍包含现有的振动取消行为，并在接口约定中说明；旧会话的定向清理不能取消新会话的提醒。
6. 将各测试类复用的 Fake 时钟、仓库、调度器、通知器和停止记录放入可定位的测试支持目录，按真实复用关系整理。
7. 删除迁移结束后无调用的兼容方法、重复常量和过时注释；不添加笼统的 `Utils` 或 `Manager` 来收纳未分类逻辑。
8. 更新 README 的目录、职责、构建命令、振动机制及现有验证边界。同步 `PROGRAM_ANALYSIS.md` 当前结论，保留明确标注的历史分析。
9. 将新一轮设备验证追加到 `DEVICE_DEBUG_REPORT.md`，包含本次 APK 指纹、实际版本、测试时间和恢复结果。

P5 本地验收：

- 全部既有和新增回归测试通过；若测试因职责拆分更名或合并，保留覆盖映射，不能仅用数量判断覆盖是否完整。
- 独立调试 APK 构建成功；Lint 无新增错误，现有 34 条警告作为对照，新增警告应解释或处理。
- 全部修改通过 `git diff --check`；文档中的文件路径和职责与代码一致。
- 测试辅助文件、APK、设备状态快照等构建产物不进入源码提交。

P5 真机验收顺序：

1. 核对当前设备连接及调试包名，保存测试前时长和需要临时改变的设备设置。
2. 使用 `-PisolatedDebug=true` 构建产物覆盖安装“休息一下·调试”，记录 APK SHA256 与设备实际安装信息。
3. 分别测试工作与休息手动振动，记录系统振动执行结果及用户触感确认。
4. 使用独立调试包进行短周期工作/休息测试。退出应用并结束后台进程，等待真实闹钟，不伪造阶段广播或更改系统时间。
5. 核对自动 WORK → BREAK → WORK 的阶段、轮数和通知；分别确认两次自动振动的执行记录与实际触感。
6. 对设备支持的勿扰和渠道限制场景核对诊断与执行；无法覆盖的场景明确标记待验证，不以普通模式成功代替。
7. 抽查通知暂停、继续、冷进程停止、旧会话请求隔离，以及停止后的通知和闹钟清理。
8. 停止短周期测试，恢复测试前时长和系统设置，重新打开页面确认状态正常，检查没有残留测试闹钟或通知。

若设备不可连接，可以完成本地阶段并交付明确的待验证列表，但最终真机项保持未完成，不能标记整个计划验收通过。

当前真机基线为 API 36。保留 API 30–32 的 AudioAttributes 振动分支及 API 33 以上的 VibrationAttributes 分支，检查编译和静态兼容性；没有实际运行过的旧系统设备不列为触感已验证。

**10. 构建、测试与证据记录方式**

在工程根目录的 PowerShell 会话中，沿用已验证的本机环境设置：

```powershell
$env:JAVA_HOME = 'C:/Program Files/Android/Android Studio/jbr'
$env:ANDROID_HOME = "$env:LOCALAPPDATA/Android/Sdk"
$env:PATH = $env:PATH.Replace('"', '')
./gradlew.bat :app:testDebugUnitTest :app:assembleDebug :app:lintDebug -PisolatedDebug=true --no-daemon --console=plain
```

以上仅调整当前命令会话，不改系统环境变量或 `local.properties`。设备地址以执行时发现的连接为准，不把历史 IP 和端口当作固定配置。

验证分配：

| 阶段 | 必要检查 |
| --- | --- |
| P0 | 确认证据与源码对应；基线改变时重新测试 |
| P1 | 策略参数化测试、原振动编排测试、全量单测、构建、Lint；设备能力字段映射验证 |
| P2 | 时长竞态与保存失败测试、全量单测、构建 |
| P3 | 阶段规则、旧数据、恢复及取消测试、全量单测、构建、Lint；持久化集成验证 |
| P4 | ViewModel 行为测试、全量单测、构建、Lint、视觉与交互对照 |
| P5 | 最终全量检查与上述真机验收 |

测试对象是业务约束和回归场景，不为单纯文件搬移或组件转发编写镜像测试。检查通过后，仅在后续修改影响相关结论时重复执行。

每阶段记录：修改文件、行为变化、执行命令、测试结果、仍未覆盖的条件、关联提交。单元测试与 Lint 报告保存在既有构建报告目录；设备证据存入新的日期/阶段目录，避免覆盖此前基线。

**11. 阶段检查与回退策略**

- 能力诊断与执行仍不一致时，先完善共同策略和测试，再继续迁移调用方。
- 提取规则导致停止保护、旧通知隔离或取消补偿测试失败时，保持阶段未完成，恢复该阶段原有执行顺序后定位差异。
- 旧数据兼容失败时保留原始夹具，修正映射，不通过清空用户数据让测试通过。
- 界面提取出现外观或交互差异时，对照 P0 修复，不顺带改变设计。
- 提交后的回退优先使用针对该阶段的反向提交；已被后续阶段依赖时按逆序处理，避免留下无法构建的中间状态。
- 真机回退使用可追溯的已验证调试产物，并记录版本、数据兼容和恢复状态。不得卸载原应用来解决调试包问题。

**12. 完成检查清单**

- [ ] 提醒能力只有一套规则来源，按用途输出不同结论，页面与执行一致。
- [ ] 工作、休息时长可独立原子修改，快速连续修改不丢值。
- [ ] 业务分支不再通过中文错误信息或提示词判断恢复与反馈类型。
- [ ] 阶段和恢复策略可独立测试，计时状态仍只有一个修改入口和一把事务锁。
- [ ] 新故障字段兼容旧数据，停止记录和通知协议兼容性得到保留。
- [ ] 主页面组件职责清楚，ViewModel 依赖显式，对外状态只读。
- [ ] 正式振动与手动自检命名明确，共用受策略约束的振动路径。
- [ ] README、分析记录、设备报告和源码描述一致。
- [ ] 全部要求的本地测试、构建与 Lint 检查完成并记录结果。
- [ ] 最新安装版本的手动及两次自动振动获得设备记录和用户触感确认。
- [ ] 测试时长、设备设置、通知和闹钟已清理或恢复。
- [ ] 每个阶段均有可审查的提交边界和验证记录；没有混入构建产物或无关修改。


**13. 执行记录**

- P0：本地基线已确认。起点为 `ee40b31`，仅 AGENTS 与计划文档未提交；67 项既有测试无失败。原 UI 实现保存在起点提交，既有截图位于 `app/build/reports/device-debug/2026-09-23`，错误状态及反馈弹窗可由该提交的 TimerScreen 参数重现。新增版本的视觉与设备映射验收仍待真机。
- P1：本地实现完成。能力读取、用途策略、诊断及实际振动已共用规则；新增策略矩阵与边界测试，合计 140 项测试通过，构建成功，Lint 0 错误、34 警告。原有振动节奏、ALARM 用途和正式通知行为保留。Android 字段映射与真机触感按用户安排延后。
- P2：本地完成。旧完整字段请求在两个可控并发场景中分别复现 45 被还原为 60、10 被还原为 5；已改为锁内合并的单字段命令。新增 6 项竞态、状态重检与失败测试，合计 146 项单测通过，构建成功。复现日志保存在构建目录 `p2-reproduction.log`，修复验证为 `p2-validation.log`。
- P3：本地完成。阶段与恢复时钟规则已提取为纯函数；恢复改用稳定故障代码，旧 ERROR 数据缺少或含未知代码均映射 UNKNOWN。Android 仓库、停止记录和时钟实现与业务接口分离；真实文件 DataStore 保存、关闭和重开验证通过。新增 13 项测试，合计 159 项通过，构建成功，Lint 0 错误、34 警告；证据为 `app/build/p3-validation.log`。事务锁与副作用顺序保持不变。
- P4：未开始。
- P5：未开始。

设备状态：开始执行时 `adb devices` 无连接。用户回复“你先完成重构，真机测试等下”；后续本地阶段继续执行，不把历史真机结果作为新版验收。
